import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  title: {
    textAlign: 'left',
    marginBottom: 5,
    fontSize: 20
  },

  description: {
    textAlign: 'left',
    marginBottom: 10,
  }
})
