import { StyleSheet } from 'react-native'
import Colors from '../../Themes/Colors';


export default StyleSheet.create({
  title: {
    textAlign: 'left',
    marginBottom: 5,
    fontSize: 20
  },

  description: {
    textAlign: 'left',
    marginBottom: 10,
  }
})
